## Messiah's telegram bot

config.toml.example:

```toml
name = "Example_bot"
port = 8080
loglevel = "debug"
admins = [1, 2]
[devices]
   [device.comp]
   macaddr = "A"
   [device.tv]
   macaddr = "B"
```
